<view a:if="{{ pageReady }}" style="width:{{style.width}};height:{{style.height}}" class="page-container">
  <canvas id="canvasTest" type="2d" onReady="onCanvasReady" ></canvas>

  <view class="playButton" onTap="play">
    play
  </view>
</view>
